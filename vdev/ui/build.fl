# This Source Form is subject to the terms of the AQUA Software License, v. 1.0.
# Copyright (c) 2025 Aymeric Wibo

import bob
import bob.go

deps = [
	# TODO Bob bug when including this guy too:
	# Assertion failed: (leaf_count <= max_child_count), function deps_build, file deps.c, line 137.
	# Dep.local("../wgpu"),
	Dep.local("../../lib"),
]

let vdriver = Go.build(["-buildmode=c-shared"])

install = {
	vdriver: "lib/vdriver/aquabsd.black.ui.vdriver",
}

run = none
